<%- include ("header") -%>
<div class="box"><p>Use as protected text or Notepad</p></div>

    <div class="box" id="heading">
      
      <h1>This is <%=kindOf%></h1>
    </div>
    <div class="box">
      <% for(item of newItems) { %>
        <form action="/delete" method="post">
      <div class="item">
        <input type="checkbox"  name="checked" value="<%=item.id%>" onChange="this.form.submit()" id="" />
        
        <input type="hidden" name="listName" value="<%=kindOf%>"/>

        <p><%=item.name%></p>
      </div>
    </form>
      <% }%>
      <form action="/" method="post">
        <input type="text" name="newItem" id="" placeholder="New Item"/>
        <button type="submit" name="listName" value="<%=kindOf%>">+</button>
      </form>
    </div>
    <%- include ("footer") -%>
